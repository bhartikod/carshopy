<nav class="navbar navbar-expand-lg bg-secondary">
  <div class="container-fluid">
    <a class="navbar-brand text-light font-italic" href="#">Carshopy</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
           <% if user_signed_in? %>
          <a class="nav-link active" aria-current="page" href="#"><%="Hi, #{current_user.phone}" %></a>
          <% end %>
        </li>
</ul> 

        </li>
      </ul>
    </div>
    <% if user_signed_in? %>
    <% "Hi, #{current_user.phone}" %>
    <%= link_to raw("<i class=\"icon-search icon-white\">Add Profile</i>"), 
          new_profile_path, class: "btn btn-success"%>
    <%= link_to raw("<i class=\"icon-search icon-white\">BuyCar</i>"), 
          cars_path, class: "btn btn-success"%>
    <%= link_to raw("<i class=\"icon-search icon-white\">sellCar</i>"), 
           new_car_path, class: "btn btn-success"%>
    <%= link_to raw("<i class=\"icon-search icon-white\">Sign Out</i>"), 
           destroy_user_session_path, method: :delete, class: "btn btn-success"%>
      <% else %>
    <%= button_to 'Sign In', new_user_session_path %>
    <%= button_to 'Sign Up', new_user_registration_path %>
<%end%>

</div>
  </div>
</nav>